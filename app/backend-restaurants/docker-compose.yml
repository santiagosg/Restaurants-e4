version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: restaurants-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: restaurants_user
      POSTGRES_PASSWORD: restaurants_password
      POSTGRES_DB: restaurants_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U restaurants_user -d restaurants_db"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
